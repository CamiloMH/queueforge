/**
 * Configuración de commitlint: exige mensajes de commit con el formato
 * Conventional Commits (p. ej. `feat: ...`, `fix(scope): ...`).
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
